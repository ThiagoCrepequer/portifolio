# Force installation of platform-specific optional dependencies
# (e.g. @rollup/rollup-linux-x64-gnu on Linux x64 CI).
#
# Without this, `npm ci` may skip optional deps due to npm CLI bug
# https://github.com/npm/cli/issues/4828, causing builds to fail in
# environments that differ from the install host's platform.
optional=true
